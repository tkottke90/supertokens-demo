import { h } from 'preact';

const LoginPage = () => {


  return(
    <main>
      <div class="card" {...{ center: '' }}>
        <h1>This is a login page</h1>
        <p>for logging in</p>
      </div>
    </main>
  )
}

export default LoginPage;